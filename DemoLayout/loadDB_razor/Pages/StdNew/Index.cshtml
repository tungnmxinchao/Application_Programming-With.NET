@page
@model loadDB_razor.Pages.StdNew.IndexModel

@{
	ViewData["Title"] = "Index";
	Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
	<a asp-page="Create">Create New</a>
</p>
<table class="table">
	<thead>
		<tr>
			<th>
				@Html.DisplayNameFor(model => model.Student[0].Name)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.Student[0].Name)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.Student[0].Gender)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.Student[0].Dob)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.Student[0].Gpa)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.Student[0].Depart)
			</th>
			<th></th>
		</tr>
	</thead>
	<tbody>
		@foreach (var item in Model.Student)
		{
			<tr>
				<td>
					@Html.DisplayFor(modelItem => item.Id)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.Name)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.Gender)



					@* <label>
						<input type="radio" name="gender_@item.Id" value="Male" @(item.Gender ? "checked" : "") disabled="disabled" />
						Male
					</label>
					<label>
						<input type="radio" name="gender_@item.Id" value="Female" @(!item.Gender ? "checked" : "") disabled="disabled" />
						Female
					</label> *@


					@* <select asp-for="@item.Gender" disabled="disabled">
						<option value="true">Male</option>
						<option value="false">Female</option>
					</select> *@

				</td>
				<td>
					@Html.DisplayFor(modelItem => item.Dob)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.Gpa)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.Depart.Id)

					@* <select asp-for="@item.DepartId" class="form-control" asp-items="Model.Departments">
						<option value="">Select Department</option>
					</select> *@


					@* @foreach (var department in Model.Departments)
					{
						<label>
							<input type="radio" name="depart_@item.Id" value="@department.Id"
							@(item.DepartId == department.Id ? "checked" : "") disabled="disabled" />
							@department.Name
						</label>
					} *@

					@* Checkboxes for department selection *@
					@foreach (var department in Model.Departments)
					{
						<label>
							<input type="checkbox" name="departments_@item.Id"
								   value="@department.Id"
							@(item.DepartId == department.Id ? "checked" : "")
								   disabled="disabled" />
							@department.Name
						</label>
						<br />
					}


				</td>
				<td>
					<a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
					<a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
					<a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
				</td>
			</tr>
		}
	</tbody>
</table>
